version: '3.9'

services:
    proxy:
        build:
            context: .
            args:
                PATH_TO_HOST_CONF: ${PATH_TO_HOST_CONF}
        environment:
            SERVER_NAME: ${SERVER_NAME}
            EMAIL: ${EMAIL}
        ports:
            - 443:443
volumes:
    certs: